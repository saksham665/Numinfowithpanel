{
  "name": "combined-api",
  "version": "1.0.0",
  "description": "Combined API for number, family and vehicle info",
  "main": "api/fetch.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@vercel/node": "^2.0.0"
  }
}
